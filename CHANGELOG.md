# Changelog

The history starts at the initial template commit. Regenerate this file from the commit log with `uv run git-cliff --output CHANGELOG.md`, and clear it when you start a project from this repository.
